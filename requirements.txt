# Install PyTorch (CPU only) FIRST, otherwise pip may pull the huge CUDA build:
#   uv pip install torch --index-url https://download.pytorch.org/whl/cpu
# Then:
#   uv pip install -r requirements.txt
torch==2.14.0

# LLM orchestration
langchain==1.4.2
langchain-core==1.6.3
langchain-community==0.4.2
langchain-text-splitters==1.1.2
langchain-huggingface==1.2.2
langchain-ollama==1.1.0
ollama==0.6.2  # the Ollama client; app/main.py imports it directly

# Vector search + embeddings + PDF parsing
faiss-cpu==1.15.1
sentence-transformers==6.1.0
pypdf==6.19.0

# API
fastapi==0.141.1
uvicorn==0.53.0
python-multipart==0.0.32

# Database
sqlalchemy==2.0.54

# Testing
pytest==9.1.1
httpx==0.28.1
